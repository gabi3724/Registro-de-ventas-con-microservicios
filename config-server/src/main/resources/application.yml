server:
  port: 8081

spring:
  application:
    name: config-server

cloud:
  config:
    server:
      git:
        uri: https://github.com/gabi3724/Registro-de-ventas-con-microservicios.git
        searchPaths: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}